import { ComponentFixture, TestBed } from '@angular/core/testing';

import { {{entity.camel}}FormComponent } from './{{entity.kebab}}-form.component';

describe('{{entity.camel}}FormComponent', () => {
  let component: {{entity.camel}}FormComponent;
  let fixture: ComponentFixture<{{entity.camel}}FormComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [{{entity.camel}}FormComponent],
    }).compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent({{entity.camel}}FormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

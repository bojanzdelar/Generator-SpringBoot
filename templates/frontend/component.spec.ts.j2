import { ComponentFixture, TestBed } from '@angular/core/testing';

import { {{entity.camel}}Component } from './{{entity.kebab}}.component';

describe('{{entity.camel}}Component}', () => {
  let component: {{entity.camel}}Component;
  let fixture: ComponentFixture<{{entity.camel}}Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [{{entity.camel}}Component],
    }).compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent({{entity.camel}}Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

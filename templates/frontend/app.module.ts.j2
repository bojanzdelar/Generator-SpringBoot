import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';

import { AppRoutingModule } from './app-routing.module';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
import { HttpClientModule, HTTP_INTERCEPTORS } from '@angular/common/http';
import { ReactiveFormsModule } from '@angular/forms';

import { AppComponent } from './app.component';
import { HomeComponent } from '@components/home/home.component';
import { MenuComponent } from '@components/menu/menu.component';
import { LoginComponent } from '@components/login/login.component';
import { TableComponent } from '@components/table/table.component';
import { AuthInterceptor } from '@interceptors/auth.interceptor';
import { HighlightRowDirective } from '@directives/highlight-row.directive';
import { EmptyTableDirective } from '@directives/empty-table.directive';

{%- for entity in entities.values() %}
import { {{entity.camel}}Component } from '@components/{{entity.kebab}}/{{entity.kebab}}.component';
import { {{entity.camel}}FormComponent } from '@components/{{entity.kebab}}/{{entity.kebab}}-form/{{entity.kebab}}-form.component';
{%- endfor %}

@NgModule({
  declarations: [
    AppComponent,
    HomeComponent,
    MenuComponent,
    LoginComponent,
    TableComponent,
    HighlightRowDirective,
    EmptyTableDirective,
    {%- for entity in entities.values() %}
    {{entity.camel}}Component,
    {{entity.camel}}FormComponent,
    {%- endfor %}
  ],
  imports: [
    BrowserModule,
    AppRoutingModule,
    BrowserAnimationsModule,
    HttpClientModule,
    ReactiveFormsModule,
  ],
  providers: [
    { provide: HTTP_INTERCEPTORS, useClass: AuthInterceptor, multi: true },
  ],
  bootstrap: [AppComponent],
})
export class AppModule {}

import { HighlightRowDirective } from './highlight-row.directive.ts';

describe('HightlightRowDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightRowDirective();
    expect(directive).toBeTruthy();
  });
});

import { EmptyTableDirective } from './empty-table.directive';

describe('EmptyTableDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyTableDirective();
    expect(directive).toBeTruthy();
  });
});
